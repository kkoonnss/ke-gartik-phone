3 HIGH risks, 7 MEDIUM risks, 7 LOW risks. Top 3 bandaids by severity: (1) try/catch require('./backgrounds') in hot path — backgrounds.js sprint artifact, all three instances need removing; (2) _initDrawScreen re-creates canvas + re-adds pointer listeners every draw phase — duplicate handlers corrupt drawings by round 5-6; (3) host.js event listeners wired at module-top before init() joins the room — architectural inconsistency from B/E agent boundary split.
